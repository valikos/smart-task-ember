import { moduleFor, test } from 'ember-qunit';

moduleFor('view:projects/project', 'Unit | View | projects/project');

// Replace this with your real tests.
test('it exists', function(assert) {
  let view = this.subject();
  assert.ok(view);
});
